 1- var is function scoped and hoister, 
 let is block-scopend and can be updated,
 const is block-scoped and canot be reasigned
 
 2. var is function scoped and hoisted
  let is blockscoped and can be updatedconst is block-scoped and cannot be reassigned.
  const is block-scoped and cannot be reassigned.
 
3. var is function-scoped and hoisted
let is block-scoped and can be updated
const is block-scoped and cannot be reasigned

